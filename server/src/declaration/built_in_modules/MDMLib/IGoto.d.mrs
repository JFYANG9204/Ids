
Namespace MDMLib

    Interface IGoto Implements Object, IEnumerable

        Sub Add(Name As String, RoutingItem As IRoutingItem)
        End Sub

        Sub Remove(Index As Variant)
        End Sub

        Sub Clear()
        End Sub

        Default ReadOnly Property Item(Index As Variant) As IRoutingItem
        ReadOnly Property Name(Index As Variant) As String
        ReadOnly Property Count() As Long

    End Interface

End Namespace

